import {IEntityConfigUserRole} from '../../../services/user/types';
import {ETypes} from '../types';


export const configEntity: IEntityConfigUserRole = {
  type: ETypes.USER_ROLE,
  attributes: {
  },
  relationships: {
  }
};
